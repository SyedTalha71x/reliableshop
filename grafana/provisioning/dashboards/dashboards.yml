apiVersion: 1

providers:
  - name: ReliableShop
    orgId: 1
    folder: ReliableShop
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards